<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-new="{{devoured}}">
		{{#if devoured}}Delete{{else}}Devour now!{{/if}}
	</button>

	{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
</li>
