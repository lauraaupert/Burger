<h2>Add a Burger</h2>
<form id="create-form">

  <div class="form-group">
    <label for="ca"></label>
    <input type="text" id="ca" name="name" placeholder="Enter the name of your dream burger">
  </div>

{{!-- If want to add the option of devouring now/later --}}
  {{!-- <div class="form-group">
    <label for="devour">Devour?</label><br>
    <input type="radio" name="sleepy" value="1" id="sleepy" checked> Sleepy!<br>
    <input type="radio" name="sleepy" value="0" id="awake"> Awake
  </div> --}}

  <button type="submit">Add Burger</button>
</form>










{{!-- Maybe for a partial to add button to each added burger? --}}
{{!-- <li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newsleep="{{devour}}">
		{{#if devour}}Devour Now!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li> --}}